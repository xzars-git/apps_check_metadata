name: apps_check_metadata
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=2.18.1 <=3.3.4"

dependencies:
  image_picker: ^1.0.2
  intl: ^0.18.1
  exif: ^3.3.0
  flutter_image_compress: ^1.1.3
  image_gallery_saver: ^2.0.3
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
